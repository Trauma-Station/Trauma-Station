- type: gameMap
  id: CentComm
  isGrid: true # Did you know that centcomm is the only "game map" that isn't actually a map? Send help.
  mapName: 'Outer Heaven'
  mapPath: /Maps/_Trauma/centcomm.yml # Trauma
  minPlayers: 10
  stations:
    centcomm:
      stationProto: NanotrasenCentralCommand
      components:
        - type: StationNameSetup
          mapNameTemplate: 'Outer Heaven'
          nameGenerator:
            !type:NanotrasenNameGenerator
            prefixCreator: 'TG'
